# Copy to ~/.config/fumox/fumox.env and adjust. The quadlet units read this
# file via EnvironmentFile= (see podman-systemd.unit(5)); after editing it,
# run `systemctl --user daemon-reload` and restart the units:
#   mkdir -p ~/.config/fumox
#   cp docker/quadlet/fumox.env.example ~/.config/fumox/fumox.env

# Admin panel login token (required — the panel is disabled without it).
FUMOX_ADMIN__TOKEN=change-me

# Optional: T2 delay-test URL. Override if the default is blocked in your
# region (default: http://www.gstatic.com/generate_204).
#FUMOX_MEOW__TEST_URL=http://cp.cloudflare.com

# Optional: allow source URLs pointing at private/loopback addresses
# (disables SSRF protection). Local testing only. Default: false.
#FUMOX_ADMIN__ALLOW_PRIVATE_URLS=true
